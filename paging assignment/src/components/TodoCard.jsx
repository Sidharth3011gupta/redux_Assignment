import React from "react";
 const TodoCard=(props)=>{
    return (
        <div>
            <h3 >ID:{props.id}</h3>
            <p >TITLE:{props.title}
            </p>
           
             </div>
    )
    };

    export default TodoCard;
